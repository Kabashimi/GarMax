<h1>Artykuły</h1>


<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <% @articles.each do |article| %>
      <div class="border">
        <%= link_to "Czytaj", articles_show_url(article.id), class: 'btn btn-primary pull-right' %>
        <h3><%= article.title %></h3>
      </div>
    <% end %>
  </div>
</div>